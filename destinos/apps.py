from django.apps import AppConfig


class DestinosConfig(AppConfig):
    """Configura la aplicación de destinos de acreditación."""
    default_auto_field = "django.db.models.BigAutoField"
    name = "destinos"
    verbose_name = "Destinos de acreditación"
